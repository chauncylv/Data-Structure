//动态数组是长度可以变化的数组，可以根据用户需要分配数组内存大小
//包含文件"array.h",在文档中可以找到
#include "array.h"
#include <stdio.h>
#include <stdlib.h>

Array array_creat(int init_size){
  Array a;
  a.array = (int *)malloc(sizeof(int)*init_size)
  a.szie = init_size;
  return a;
}
